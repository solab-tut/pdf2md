# Local LLM (Ollama) — point to your external Ollama instance
# Use a LAN IP if Ollama runs in a separate Docker stack (rootless Docker cannot reach host via host.docker.internal)
# e.g. OLLAMA_URL=http://192.168.0.x:11434  or  http://192.168.0.x:3102  (if port-mapped)
OLLAMA_URL=http://192.168.0.x:11434
PDF2MD_MODEL=qwen2.5vl:32b

# Azure OpenAI (uncomment and fill to enable Azure models in the UI)
# AZURE_OPENAI_ENDPOINT=https://your-instance.openai.azure.com
# AZURE_OPENAI_API_KEY=your-key-here
# AZURE_OPENAI_DEPLOYMENTS=gpt-4o
# AZURE_OPENAI_API_VERSION=2024-10-21

# OpenAI-compatible endpoint (OpenAI, LM Studio, LocalAI, vLLM, etc.)
# Available models are fetched from /v1/models and filtered by vision capability.
# OPENAI_MODELS is used as a fallback when the API call fails or no vision models are detected.
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_API_KEY=your-key-here
# OPENAI_MODELS=gpt-4o,gpt-4o-mini

# Common settings
PDF2MD_DEFAULT_DPI=150
PDF2MD_DEFAULT_THINKING=off
PDF2MD_MAX_IMAGE_EDGE=1800
PDF2MD_JPEG_QUALITY=75
PDF2MD_MAX_TOKENS=8192
PDF2MD_OLLAMA_KEEP_ALIVE=15m
PDF2MD_OLLAMA_TIMEOUT=600
PDF2MD_OLLAMA_LOAD_TIMEOUT=300
PDF2MD_PAGE_ASSET_DPI=200
PDF2MD_PAGE_ASSET_JPEG_QUALITY=90
